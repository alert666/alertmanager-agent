name: Build and Push Docker Image

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    environment: PROD
    permissions: write-all

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Generate Image Tag
        id: vars
        run: |
          BRANCH=$(echo "${GITHUB_REF_NAME}" | tr '[:upper:]' '[:lower:]' | sed 's#[/ ]#-#g')
          SHORT_SHA=$(git rev-parse --short=7 HEAD)
          DATE=$(date +'%Y%m%d-%H%M%S')
          IMAGE_TAG="${BRANCH}-${SHORT_SHA}-${DATE}"
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "Generated tag: $IMAGE_TAG"

      - name: Login to Gongjiyun Container Registry
        uses: docker/login-action@v3
        with:
          registry: privateharbor.suanleme.cn
          username: ${{ secrets.GONGJIYUN_DOCKER_USER }}
          password: ${{ secrets.GONGJIYUN_DOCKER_PASS }}

      - name: Log in to Aliyun Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.DOCKER_HUB }}
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          platforms: linux/amd64,linux/arm64
          context: .
          file: Dockerfile
          build-args: |
            GO_VERSION=1.26.5
            MAIN_PATH=main.go
          push: true
          tags: |
            ${{ vars.DOCKER_HUB }}/qqlx/alertmanager-agent:${{ steps.vars.outputs.IMAGE_TAG }}
            privateharbor.suanleme.cn/k8s/alertmanager-agent:${{ steps.vars.outputs.IMAGE_TAG }}
            ghcr.io/${{ github.repository }}:${{ steps.vars.outputs.IMAGE_TAG }}
            ghcr.io/${{ github.repository }}:latest

      - name: Output Image Info
        run: |
          echo "Image pushed: ${{ vars.DOCKER_HUB }}/qqlx/alertmanager-agent:${{ steps.vars.outputs.IMAGE_TAG }}"
          echo "Image pushed to privateharbor.suanleme.cn: privateharbor.suanleme.cn/k8s/alertmanager-agent:${{ steps.vars.outputs.IMAGE_TAG }}"
          echo "Image pushed to GitHub Container Registry: ghcr.io/${{ github.repository }}:${{ steps.vars.outputs.IMAGE_TAG }}"
          echo "Image pushed to GitHub Container Registry: ghcr.io/${{ github.repository }}:latest"
